# ---------------------------------------------------------------------------
# Demand Sensing Intelligence Platform - backend dependencies
# Python 3.11+ (developed and verified on 3.13.5)
# Every dependency below is free-tier / open-source; no paid service required.
# ---------------------------------------------------------------------------

# --- Web layer -------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.53.0
sse-starlette==3.0.2

# --- Configuration ---------------------------------------------------------
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Agentic orchestration -------------------------------------------------
langgraph==1.2.11
langchain-core==1.6.3
langchain-google-genai==4.4.0     # Gemini binding (chat + tool calling)
google-genai==2.23.0

# --- Retrieval -------------------------------------------------------------
faiss-cpu==1.15.0                 # vector index for the internal library
numpy==2.5.3

# --- Knowledge graph -------------------------------------------------------
networkx==3.6.1                   # in-process graph store (no server needed)

# --- External signals ------------------------------------------------------
tavily-python==0.8.3              # live web/news search
httpx==0.28.1                     # GDELT + Wikidata REST calls

# --- Utilities -------------------------------------------------------------
tenacity==9.1.4                   # retry/backoff for free-tier rate limits
orjson==3.12.0
